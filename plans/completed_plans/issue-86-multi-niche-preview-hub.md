# Issue 86 - Multi-Niche Preview Hub

## Goal

Convert the existing roofing-only preview dashboard into a generic `local-growth-preview` hub that can organize demos across roofing, restaurants, HVAC, plumbing, and future niches.

## Scope

In scope:

- Audit current Next.js dashboard, routing, and prospect data structure.
- Add a demo registry with niche, status, current-focus, and route metadata.
- Update the dashboard UI with niche filters and a current-focus section.
- Rename generic app/dashboard copy to `local-growth-preview`.
- Keep Charger Roofing's direct URL working if feasible.
- Add Pizabella as the first restaurant demo route entry if practical.
- Update README/app metadata to explain the multi-niche hub.

Out of scope:

- Sending outreach.
- Reworking every roofing prospect page.
- Renaming the Vercel project/domain before UI validation.
- Building full HVAC/plumbing demo pages in this issue.

## Checklist

- [x] Create GitHub issue and implementation branch.
- [x] Audit current dashboard, routes, prospect registry, metadata, and README.
- [x] Add demo registry and metadata.
- [x] Add Pizabella restaurant preview route.
- [x] Update dashboard filters, current-focus panel, and generic naming.
- [x] Update app metadata/package/docs to `local-growth-preview`.
- [x] Validate TypeScript/build and browser-render dashboard.
- [x] Commit, push, and open PR.
- [x] Post issue update.

## Implementation Notes

- Keep the existing `ProspectData` objects stable to avoid rewriting every roofing data file.
- Add a higher-level demo registry that wraps roofing prospects with niche/status metadata.
- Keep root-level prospect slugs like `/charger-roofing` working.
- Use `/pizabella` for the first restaurant preview route.
- Treat Vercel project rename as a follow-up after the generic UI is verified; changing the project name may affect deployment URLs.

## Validation

- `npm run build` passed in `EXPERIMENTS/001-roofing-landing-page-service/product/demo-app`.
- `git diff --check` passed.
- Playwright CLI screenshots rendered `/dashboard` desktop/mobile and `/pizabella`.
- `/charger-roofing` remains generated by the existing `[slug]` route.
